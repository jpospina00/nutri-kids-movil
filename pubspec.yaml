name: nutri_kids_movil
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  fluro: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  flutter_spinkit: ^5.2.2
  flutter_svg: ^2.0.9
  animated_text_kit: ^4.2.2
  google_fonts: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
